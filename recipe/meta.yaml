{% set version = "8.1.1" %}
{% set name = "genutil" %}

package:
    name: {{ name|lower }}
    version: {{ version }}

source:
    url: https://github.com/cdat/genutil/archive/v{{ version }}.tar.gz
    sha256: b544e33380d7814bb10cfb797a71b9a37e9a6a737a763dddc40e9e327c5202b4

build:
    number: 0
    skip: true  # [win]
 
requirements:
    build:
        - {{ compiler("c") }}
    host:
        - python {{ python }}
        - setuptools
        - numpy
        - udunits2
        - cdms2 >=3.1.2
        - libcdms >=3.1.2
        - cdat_info >=8.1.1
    run:
        - python {{ python }}
        - cdms2 >=3.1.2
        - libcdms >=3.1.2
        - udunits2
        - cdat_info >=8.1.1
        - {{ pin_compatible('numpy') }}

test:
    commands:
        -  export CDAT_ANONYMOUS_LOG=false && python -c "import genutil"

about:
    home:  http://github.com/CDAT/genutil
    license: BSD
    license_file: bsd-3-clause.txt
    summary: 'General Utitilites for the Community Data Analysys Tools'

extra:
    recipe-maintainers:
        - doutriaux1
        - dnadeau4
        - xylar
